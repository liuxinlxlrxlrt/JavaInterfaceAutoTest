server:
  port: 8888

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/course?serverTimezone=UTC
    userName: root
    password: lx@lx19870613

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/*